# crm portal

Product-owned code for the **crm** portal.

- Prefer new files under this folder (`components/`, `lib/`, …)
- Routes: `src/app/crm/` (or mapped public routes — see `src/PORTALS.md`)
- Shared suite only: `src/suite/`
- Do not import other portals

Compatibility: `@/lib/crm` / `@/components/crm` symlink here when applicable.
