"use client";

/** Index redirects are handled by `layout.tsx` to the first permitted report. */
export default function ReportsIndexPage() {
  return (
    <div className="flex min-h-[40vh] items-center justify-center text-sm text-[var(--text-muted)]">
      Loading reports…
    </div>
  );
}
