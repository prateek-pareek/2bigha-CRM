{
  "name": "2bigha-crm",
  "version": "1.0.0",
  "private": true,
  "description": "2Bigha CRM",
  "scripts": {
    "dev:portal": "cd portal && npm run dev",
    "dev:api": "cd api && npm run start:dev",
    "dev:all": "concurrently \"npm run dev:portal\" \"npm run dev:api\"",
    "dev": "npm run dev:all",
    "build:all": "concurrently \"cd portal && npm run build\" \"cd api && npm run build\"",
    "db:up": "docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d mongodb redis",
    "setup": "npm install && cd portal && npm install --legacy-peer-deps && cd ../api && npm install",
    "db:down": "docker compose stop mongodb redis"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
