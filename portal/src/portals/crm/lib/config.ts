/**
 * CRM API base URL — owned by CRM so product code does not import suite config for CRM calls.
 * Override with NEXT_PUBLIC_CRM_API_URL when CRM is deployed on its own host.
 */
export { CRM_API_URL } from "@/lib/api/config";
